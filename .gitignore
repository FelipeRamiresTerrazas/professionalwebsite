# Datasets grandes — não podem ser versionados no GitHub (>50MB)
creditcard.csv
kaggle/foodpanda/
kaggle/search_flights/
kaggle/test_a-b_delivery_hero

# Modelo treinado (pode ser grande)
*.joblib
*.pkl

# Gráficos gerados pelo script — track PNGs for GitHub Pages, ignore large files
# figures/   ← intentionally NOT ignored so images are served via Pages

# Python
__pycache__/
*.pyc
*.pyo
.env
.venv/
